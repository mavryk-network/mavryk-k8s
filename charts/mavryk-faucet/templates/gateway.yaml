{{- if .Values.gateway.enabled -}}
{{- $_ := required "gateway.host is required when gateway.enabled is true" .Values.gateway.host -}}
{{- $fullName := "mavryk-faucet" -}}
{{- $useTLS := ne (.Values.gateway.tlsSecretName | toString) "" -}}
{{- $section := ternary "https" "http" $useTLS -}}
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ $fullName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
    {{- with .Values.gateway.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.gateway.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  gatewayClassName: {{ .Values.gateway.className | default "envoy" }}
  listeners:
    {{- if $useTLS }}
    - name: https
      hostname: {{ .Values.gateway.host | quote }}
      port: 443
      protocol: HTTPS
      tls:
        mode: Terminate
        certificateRefs:
          - kind: Secret
            name: {{ .Values.gateway.tlsSecretName | quote }}
    {{- else }}
    - name: http
      hostname: {{ .Values.gateway.host | quote }}
      port: 80
      protocol: HTTP
    {{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $fullName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $fullName }}
      sectionName: {{ $section }}
  hostnames:
    - {{ .Values.gateway.host | quote }}
  rules:
    {{- range $path := list "/info" "/challenge" "/verify" }}
    - matches:
        - path:
            type: Exact
            value: {{ $path }}
      backendRefs:
        - name: {{ $fullName }}
          port: 3000
    {{- end }}
    - matches:
        - path:
            type: PathPrefix
            value: /status
      backendRefs:
        - name: {{ $fullName }}
          port: 3000
    {{- if .Values.enableUI }}
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ $fullName }}
          port: 8080
    {{- end }}
{{- if .Values.gateway.cors.enabled }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: {{ $fullName }}-cors
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $fullName }}
  cors:
    {{- with .Values.gateway.cors.allowOrigins }}
    allowOrigins:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.cors.allowMethods }}
    allowMethods:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.cors.allowHeaders }}
    allowHeaders:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.cors.exposeHeaders }}
    exposeHeaders:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.cors.maxAge }}
    maxAge: {{ . | quote }}
    {{- end }}
    allowCredentials: {{ .Values.gateway.cors.allowCredentials }}
{{- end }}
{{- $bt := or .Values.gateway.rateLimit.enabled .Values.gateway.timeout.enabled .Values.gateway.connection.enabled }}
{{- if $bt }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: {{ $fullName }}-backend-traffic
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $fullName }}
  {{- if .Values.gateway.rateLimit.enabled }}
  rateLimit:
    type: Local
    local:
      rules:
        {{- toYaml .Values.gateway.rateLimit.rules | nindent 8 }}
  {{- end }}
  {{- if .Values.gateway.timeout.enabled }}
  timeout:
    {{- with .Values.gateway.timeout.tcp }}
    tcp:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.timeout.http }}
    http:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
  {{- if .Values.gateway.connection.enabled }}
  connection:
    bufferLimit: {{ .Values.gateway.connection.bufferLimit | quote }}
  {{- end }}
{{- end }}
{{- if .Values.gateway.client.enabled }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: {{ $fullName }}-client-traffic
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $fullName }}
  connection:
    bufferLimit: {{ .Values.gateway.client.bufferLimit | quote }}
{{- end }}
{{- end }}

{{- if .Values.gateway.backend.enabled }}
{{- $_ := required "gateway.backend.host is required when gateway.backend.enabled is true" .Values.gateway.backend.host -}}
{{- $fullName := "mavryk-faucet" -}}
{{- $useTLS := ne (.Values.gateway.backend.tlsSecretName | toString) "" }}
{{- $section := ternary "https" "http" $useTLS }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ $fullName }}-backend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
    {{- with .Values.gateway.backend.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.gateway.backend.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  gatewayClassName: {{ .Values.gateway.backend.className | default "envoy" }}
  listeners:
    {{- if $useTLS }}
    - name: https
      hostname: {{ .Values.gateway.backend.host | quote }}
      port: 443
      protocol: HTTPS
      tls:
        mode: Terminate
        certificateRefs:
          - kind: Secret
            name: {{ .Values.gateway.backend.tlsSecretName | quote }}
    {{- else }}
    - name: http
      hostname: {{ .Values.gateway.backend.host | quote }}
      port: 80
      protocol: HTTP
    {{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $fullName }}-backend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $fullName }}-backend
      sectionName: {{ $section }}
  hostnames:
    - {{ .Values.gateway.backend.host | quote }}
  rules:
    {{- range $path := list "/info" "/challenge" "/verify" }}
    - matches:
        - path:
            type: Exact
            value: {{ $path }}
      backendRefs:
        - name: {{ $fullName }}
          port: 3000
    {{- end }}
    - matches:
        - path:
            type: PathPrefix
            value: /status
      backendRefs:
        - name: {{ $fullName }}
          port: 3000
{{- if .Values.gateway.backend.cors.enabled }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: {{ $fullName }}-backend-cors
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $fullName }}-backend
  cors:
    {{- with .Values.gateway.backend.cors.allowOrigins }}
    allowOrigins:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.backend.cors.allowMethods }}
    allowMethods:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.backend.cors.allowHeaders }}
    allowHeaders:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.backend.cors.exposeHeaders }}
    exposeHeaders:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.backend.cors.maxAge }}
    maxAge: {{ . | quote }}
    {{- end }}
    allowCredentials: {{ .Values.gateway.backend.cors.allowCredentials }}
{{- end }}
{{- $btb := or .Values.gateway.backend.rateLimit.enabled .Values.gateway.backend.timeout.enabled .Values.gateway.backend.connection.enabled }}
{{- if $btb }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: {{ $fullName }}-backend-backend-traffic
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $fullName }}-backend
  {{- if .Values.gateway.backend.rateLimit.enabled }}
  rateLimit:
    type: Local
    local:
      rules:
        {{- toYaml .Values.gateway.backend.rateLimit.rules | nindent 8 }}
  {{- end }}
  {{- if .Values.gateway.backend.timeout.enabled }}
  timeout:
    {{- with .Values.gateway.backend.timeout.tcp }}
    tcp:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .Values.gateway.backend.timeout.http }}
    http:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
  {{- if .Values.gateway.backend.connection.enabled }}
  connection:
    bufferLimit: {{ .Values.gateway.backend.connection.bufferLimit | quote }}
  {{- end }}
{{- end }}
{{- if .Values.gateway.backend.client.enabled }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: {{ $fullName }}-backend-client-traffic
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mavryk-faucet.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $fullName }}-backend
  connection:
    bufferLimit: {{ .Values.gateway.backend.client.bufferLimit | quote }}
{{- end }}
{{- end }}
